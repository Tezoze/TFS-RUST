-- Phase 1b: `getPositions` and `getTargets` are native on `CombatRef`
-- (`crates/tfs-rust-lua/src/userdata/combat.rs`). Previously these used
-- TARGETTILE / TARGETCREATURE callbacks + `:execute`; native resolution
-- avoids side effects and matches C++ `luaCombatGetPositions` /
-- `luaCombatGetTargets`.
